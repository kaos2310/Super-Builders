name: Build patched Android 16 crosvm (AVF Gunyah IRQFD)

on:
  workflow_dispatch:
  push:
    branches:
      - avf-crosvm-irq16
    paths:
      - '.github/workflows/build-crosvm-avf-irq16.yml'
      - 'patches/crosvm/android-16.0.0_r4-gunyah-irq16.patch'
      - 'scripts/patch-crosvm-gunyah-irqfd.py'

permissions:
  contents: read

jobs:
  build-crosvm:
    runs-on: ubuntu-24.04
    timeout-minutes: 330
    env:
      BUILD_VOLUME: /opt/aosp-build
      AOSP_DIR: /opt/aosp-build/aosp
      OUT_DIR: /opt/aosp-build/out
      AOSP_TAG: android-16.0.0_r4
      PATCH_FILE: ${{ github.workspace }}/patches/crosvm/android-16.0.0_r4-gunyah-irq16.patch

    steps:
      - name: Expand runner build volume
        uses: easimon/maximize-build-space@c28619d8999a147d5e09c1199f84ff6af6ad5794
        with:
          build-mount-path: /opt/aosp-build
          root-reserve-mb: 4096
          temp-reserve-mb: 256
          swap-size-mb: 2048
          remove-dotnet: 'true'
          remove-android: 'true'
          remove-haskell: 'true'
          remove-codeql: 'true'
          remove-docker-images: 'true'

      - name: Checkout Super-Builders
        uses: actions/checkout@v4

      - name: Verify expanded runner storage
        shell: bash
        run: |
          set -euxo pipefail
          df -hT
          df -hT "$BUILD_VOLUME"
          mount | grep -F "$BUILD_VOLUME" || true
          test -d "$BUILD_VOLUME"
          test -w "$BUILD_VOLUME"
          sudo apt-get clean

      - name: Install repo tool
        shell: bash
        run: |
          set -euxo pipefail
          mkdir -p "$HOME/bin"
          curl -fsSL https://storage.googleapis.com/git-repo-downloads/repo -o "$HOME/bin/repo"
          chmod +x "$HOME/bin/repo"
          echo "$HOME/bin" >> "$GITHUB_PATH"
          "$HOME/bin/repo" version

      - name: Initialize Android 16 r4 manifest
        shell: bash
        run: |
          set -euxo pipefail
          mkdir -p "$AOSP_DIR" "$OUT_DIR"
          cd "$AOSP_DIR"
          repo init \
            -u https://android.googlesource.com/platform/manifest \
            -b "$AOSP_TAG" \
            --depth=1 \
            --partial-clone \
            --clone-filter=blob:none \
            --no-clone-bundle
          cp .repo/manifests/default.xml /tmp/aosp-manifest.xml
          test -s /tmp/aosp-manifest.xml
          df -hT "$BUILD_VOLUME"

      - name: Sync minimal crosvm build source set
        shell: bash
        run: |
          set -euxo pipefail
          cd "$AOSP_DIR"

          python3 - <<'PY' > /tmp/crosvm-projects.txt
          import xml.etree.ElementTree as ET

          root = ET.parse('/tmp/aosp-manifest.xml').getroot()
          prefixes = (
              'bionic',
              'build/blueprint', 'build/make', 'build/soong', 'build/release',
              'device/generic',
              'external/crosvm',
              'external/rust/',
              'external/golang-protobuf', 'external/starlark-go',
              'external/go-cmp', 'external/pogreb',
              'external/minijail', 'external/libcap', 'external/libdrm',
              'external/libepoxy', 'external/libffi', 'external/libusb',
              'external/protobuf', 'external/boringssl', 'external/zlib',
              'external/wayland', 'external/wayland-protocols',
              'external/mesa3d', 'external/gfxstream-protocols',
              'frameworks/native',
              'hardware/interfaces',
              'packages/modules/Virtualization',
              'prebuilts/bazel/common',
              'prebuilts/build-tools', 'prebuilts/clang/host/linux-x86',
              'prebuilts/devtools', 'prebuilts/go/linux-x86', 'prebuilts/jdk/jdk21',
              'prebuilts/misc', 'prebuilts/ndk', 'prebuilts/remoteexecution-client',
              'prebuilts/rust', 'prebuilts/sdk', 'prebuilts/tools',
              'prebuilts/vndk',
              'system/core', 'system/libbase', 'system/logging',
              'system/sepolicy', 'system/tools/aidl',
          )
          selected = []
          for p in root.findall('project'):
              path = p.get('path') or p.get('name') or ''
              if any(path == x or path.startswith(x.rstrip('/') + '/') for x in prefixes):
                  selected.append(p.get('name'))
          selected = sorted(set(x for x in selected if x))
          if not selected:
              raise SystemExit('minimal manifest selector matched zero projects')
          print('\n'.join(selected))
          PY

          echo "Selected $(wc -l < /tmp/crosvm-projects.txt) AOSP projects"
          cat /tmp/crosvm-projects.txt
          grep -q '^platform/prebuilts/vndk/' /tmp/crosvm-projects.txt
          mapfile -t PROJECTS < /tmp/crosvm-projects.txt
          repo sync -c -j4 --no-tags --no-clone-bundle --force-sync "${PROJECTS[@]}"

          echo '=== STORAGE AFTER MINIMAL SYNC ==='
          df -hT /
          df -hT "$BUILD_VOLUME"
          du -sh "$AOSP_DIR" "$OUT_DIR" 2>/dev/null || true
          test -d external/crosvm
          test -d build/soong
          test -d build/release
          test -d external/golang-protobuf/proto
          test -d external/starlark-go/starlark
          test -d external/go-cmp/cmp
          test -d external/pogreb
          test -x prebuilts/go/linux-x86/bin/go
          test -x prebuilts/jdk/jdk21/linux-x86/bin/java
          test -d prebuilts/rust
          find prebuilts/vndk -mindepth 1 -maxdepth 1 -type d -print -quit | grep -q .

      - name: Verify exact crosvm source revision
        shell: bash
        run: |
          set -euxo pipefail
          cd "$AOSP_DIR/external/crosvm"
          echo "crosvm HEAD=$(git rev-parse HEAD)"
          test "$(git rev-parse HEAD)" = "738b7016fc50d6eadc94c8ca28022ad7c21f798d"
          grep -n -E 'AARCH64_IRQ_BASE|AARCH64_VMWDT_IRQ|let has_bios' aarch64/src/lib.rs
          grep -n 'pub fn register_irqfd' hypervisor/src/gunyah/mod.rs
          grep -q 'const AARCH64_IRQ_BASE: u32 = 4;' aarch64/src/lib.rs
          grep -q 'const AARCH64_VMWDT_IRQ: u32 = 15;' aarch64/src/lib.rs

      - name: Apply Gunyah IRQFD collision patch
        shell: bash
        run: |
          set -euxo pipefail
          python3 "$GITHUB_WORKSPACE/scripts/patch-crosvm-gunyah-irqfd.py" "$AOSP_DIR/external/crosvm"
          cd "$AOSP_DIR/external/crosvm"
          grep -n -E 'AARCH64_IRQ_BASE|AARCH64_VMWDT_IRQ|reserve_irq' aarch64/src/lib.rs
          grep -q 'const AARCH64_IRQ_BASE: u32 = 4;' aarch64/src/lib.rs
          grep -q 'const AARCH64_VMWDT_IRQ: u32 = 15;' aarch64/src/lib.rs
          grep -q 'reserve_irq(AARCH64_VMWDT_IRQ)' aarch64/src/lib.rs
          grep -n 'GUNYAH IRQFD' hypervisor/src/gunyah/mod.rs
          grep -q 'GUNYAH IRQFD add:' hypervisor/src/gunyah/mod.rs
          grep -q 'GUNYAH IRQFD add failed:' hypervisor/src/gunyah/mod.rs
          git diff --check
          git diff -- aarch64/src/lib.rs hypervisor/src/gunyah/mod.rs

      - name: Build ARM64 Android crosvm
        shell: bash
        run: |
          set -euxo pipefail
          cd "$AOSP_DIR"
          export OUT_DIR="$OUT_DIR"
          export ENVSETUP_NO_COMPLETION=1
          set +u
          source build/envsetup.sh
          lunch aosp_arm64 trunk_staging eng
          m -j2 crosvm
          set -u
          echo '=== STORAGE AFTER BUILD ==='
          df -hT "$BUILD_VOLUME"
          du -sh "$OUT_DIR" 2>/dev/null || true

      - name: Collect and validate crosvm binary
        shell: bash
        run: |
          set -euxo pipefail
          cd "$AOSP_DIR"
          mkdir -p "$GITHUB_WORKSPACE/artifacts/crosvm-avf-gunyah-irqfd"
          CANDIDATES="$(find "$OUT_DIR/target/product" -type f -name crosvm -perm -111 2>/dev/null || true)"
          printf '%s\n' "$CANDIDATES"
          CROSVM="$(printf '%s\n' "$CANDIDATES" | grep '/system/bin/crosvm$' | head -n1 || true)"
          if [ -z "$CROSVM" ]; then CROSVM="$(printf '%s\n' "$CANDIDATES" | head -n1)"; fi
          test -n "$CROSVM"
          test -f "$CROSVM"
          file "$CROSVM"
          file "$CROSVM" | grep -qi 'ARM aarch64\|ARM64'
          "$CROSVM" version 2>&1 | tee "$GITHUB_WORKSPACE/artifacts/crosvm-avf-gunyah-irqfd/version.txt" || true
          sha256sum "$CROSVM" | tee "$GITHUB_WORKSPACE/artifacts/crosvm-avf-gunyah-irqfd/sha256.txt"
          readelf -n "$CROSVM" | tee "$GITHUB_WORKSPACE/artifacts/crosvm-avf-gunyah-irqfd/readelf-notes.txt" || true
          cp -a "$CROSVM" "$GITHUB_WORKSPACE/artifacts/crosvm-avf-gunyah-irqfd/crosvm"
          cp "$PATCH_FILE" "$GITHUB_WORKSPACE/artifacts/crosvm-avf-gunyah-irqfd/"
          cp "$GITHUB_WORKSPACE/scripts/patch-crosvm-gunyah-irqfd.py" "$GITHUB_WORKSPACE/artifacts/crosvm-avf-gunyah-irqfd/"
          cat > "$GITHUB_WORKSPACE/artifacts/crosvm-avf-gunyah-irqfd/build-info.txt" <<EOF
          AOSP_TAG=$AOSP_TAG
          CROSVM_SOURCE_COMMIT=$(cd external/crosvm && git rev-parse HEAD)
          PATCH=RESERVE_VMWDT_IRQ15_PLUS_GUNYAH_IRQFD_LOGGING
          PATCH_METHOD=SOURCE_ANCHORS
          TARGET=aosp_arm64-trunk_staging-eng
          OUT_DIR=$OUT_DIR
          SOURCE_BINARY=$CROSVM
          EOF
          ls -lh "$GITHUB_WORKSPACE/artifacts/crosvm-avf-gunyah-irqfd"

      - name: Upload patched crosvm artifact
        uses: actions/upload-artifact@v4
        with:
          name: crosvm-android16-r4-gunyah-irqfd-arm64
          path: artifacts/crosvm-avf-gunyah-irqfd/
          if-no-files-found: error
          retention-days: 14
