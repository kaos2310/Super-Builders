name: ReSukiSU KMI Module Loader Audit

permissions:
  contents: write
  actions: write

on:
  workflow_dispatch:
  push:
    branches:
      - agent/kmi-module-loader-audit
    paths:
      - .github/workflows/run-kmi-module-loader-audit.yml

# Retry 4: bind the release ancestry check to the immutable pinned commit.
jobs:
  dependencies:
    uses: ./.github/workflows/cache-dependencies.yml

  build:
    needs: dependencies
    uses: ./.github/workflows/build-resukisu.yml
    with:
      android_version: android14
      kernel_version: "6.1"
      sub_level: "145"
      os_patch_level: "2025-09"
      add_susfs: true
      add_zeromount: true
      add_droidspaces: true
      add_zram: true
      add_bbg: true
      add_overlayfs_support: true
      add_kpm: false
      susfs_open_redirect: false
      add_ptrace_fix: true
      add_perf_patches: true
      resukisu_source_branch: "390f611616089c2079a29559b66e87b37df610cb"
      resukisu_version_code: "35047"
      device_codename: e3q
