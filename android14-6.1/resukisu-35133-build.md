# ReSukiSU 35133 / S928B ZZHL EUX

Kernel baseline: [successful run 34501538739](https://github.com/kaos2310/Super-Builders/actions/runs/34501538739),
Super-Builders `206b5240d0047a4a4f866ac6fcf022c55c678365`.
The branch also contains the unchanged ZeroMount module workflow and three
patch helpers from Super-Builders `3d8b9b153d9333daef5374d2aaa7183c98ee40e0`.

| Component | Exact source |
| --- | --- |
| ReSukiSU | `6930e97b59f8f5a7a2e75583f7be1cf982856157`; `30700 + 4433 = 35133` |
| SUSFS v2.3.0 | `153f88df3be2501d2d33364f8fe05247aecb3cef`; Android 14 / 6.1 branch checked on 2026-09-11 |
| ZeroMount module | `v2.0.216-dev.guard2.brene66`; upstream `c3cb7ffdf749f2441928ec47798615a4c79fab65` plus the unchanged reference port |

Use the existing ZeroMount module from [successful run 34512261330](https://github.com/kaos2310/Super-Builders/actions/runs/34512261330).
Artifact ID: `10166435393`. The inner installable ZIP has SHA-256
`3758743f81e40efd222244acc8dd4f44f62bd95e4dd77ba718d2b3a768fe092c`.
The kernel-side ZeroMount patch and its exclusion of the overlapping `task_mmu`
hook are retained. The userspace module remains a separate installable artifact.

The strict workflow first requires an ARM64 Android build of `ksuinit` and `ksud`
using the upstream Nightly approach, `rust-src`, NDK r29 and Android API 26.
Both projects use Edition 2024, so the effective compiler floor is Rust 1.85,
above the requested Rust 1.71 minimum. Each build uses `--locked` with the
respective `Cargo.lock` from the exact ReSukiSU pin. Manifests and lockfiles are
compared byte-for-byte with Git before and after compilation; no older lockfile
is restored. Host Clang 18 and Libclang 18 are installed explicitly. A real
Libclang index is created before compiling, and the final gate requires the
bindings generated by locked `bindgen 0.73.1`.

`ReSukiSU-35133-ARM64-ksud-ksuinit` contains both ELF64 AArch64 binaries, both
lockfiles, compiler/source/build identity and SHA-256 checksums. `ksuinit` is
also copied into the ARM64 assets before building `ksud`. No LKM payloads are
built for this built-in e3q kernel.

The UAPI4 scoped-session adapter is carried forward from the kernel reference.
Its affected source files are unchanged between upstream 35129 and 35133.
Local checks passed 241 production-C checks, 14 WebView checks, AArch64 object
compilation, nine negative controls, wrong-pin rejection and repeat-application
rollback. The strict kernel job additionally requires final configuration,
compiled-symbol, Image-version and Samsung DLKM/KMI package audits.

KernelSU setup continues to receive the immutable commit as its argument.
The new optional submodule setup mode is not enabled. Auto-Inline-Hook PR #261
is not merged or cherry-picked into this build. This update does not establish
boot or device-runtime results for the new binaries.
