diff --git a/kernel/selinux/sepolicy.c b/kernel/selinux/sepolicy.c
index 5d3bb2f1..b9f4d7a1 100644
--- a/kernel/selinux/sepolicy.c
+++ b/kernel/selinux/sepolicy.c
@@ -2,6 +2,7 @@
 #include <linux/printk.h>
 #include <linux/slab.h>
+#include <linux/string.h>
 #include <linux/version.h>
 #include <linux/vmalloc.h>
 #include <linux/mutex.h>
@@ -208,10 +209,21 @@ static bool add_rule(struct policydb *db, const char *s, const char *t, const cha
         }
     }
 
-    if (t) {
+    if (t && !strcmp(t, "self")) {
+        if (!src) {
+            pr_info("target self requires an explicit source type\n");
+            return false;
+        }
+        tgt = src;
+    } else if (t) {
         tgt = symtab_search(&db->p_types, t);
         if (tgt == NULL) {
+            /* Some Samsung Android 17 policies do not expose an nsfs type.
+             * A rule targeting a type that does not exist cannot affect the
+             * active policy, so treat that optional compatibility rule as a
+             * no-op instead of failing the complete module policy batch.
+             */
+            if (!strcmp(t, "nsfs")) {
+                pr_info("optional target type nsfs does not exist; skipping rule\n");
+                return true;
+            }
             pr_info("target type %s does not exist\n", t);
             return false;
         }
diff --git a/kernel/supercall/dispatch.c b/kernel/supercall/dispatch.c
index 30fc138a..fc0cd79a 100644
--- a/kernel/supercall/dispatch.c
+++ b/kernel/supercall/dispatch.c
@@ -650,6 +650,14 @@ static int do_dynamic_manager(void __user *arg)
         return -EFAULT;
     }
 
+    /* The manager UI performs GET directly through the inherited control FD.
+     * Keep all mutating operations root-only while allowing the authenticated
+     * manager process to read the current dynamic-manager configuration.
+     */
+    if (cmd.operation != DYNAMIC_MANAGER_OP_GET && ksu_get_uid_t(current_uid()) != 0) {
+        pr_warn("dynamic_manager: operation %u requires root uid=%d\n",
+                cmd.operation, ksu_get_uid_t(current_uid()));
+        return -EPERM;
+    }
+
     int ret = ksu_handle_dynamic_manager(&cmd);
     if (ret)
         return ret;
@@ -1035,7 +1043,7 @@ static const struct ksu_ioctl_cmd_map ksu_ioctl_handlers[] = {
         .cmd = KSU_IOCTL_DYNAMIC_MANAGER,
         .name = "SET_DYNAMIC_MANAGER",
         .handler = do_dynamic_manager,
-        .perm_check = only_root 
+        .perm_check = manager_or_root 
     },
     { 
         .cmd = KSU_IOCTL_GET_MANAGERS, 
